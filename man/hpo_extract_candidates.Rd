% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harness.R
\name{hpo_extract_candidates}
\alias{hpo_extract_candidates}
\title{Extract FastHPOCR candidate tables for one or more cases}
\usage{
hpo_extract_candidates(
  annotator,
  text,
  case_id = NULL,
  longest_match = TRUE,
  source = "FastHPOCR",
  run_id = NA_character_
)
}
\arguments{
\item{annotator}{A \code{fast_hpo_cr_annotator} returned by \code{\link[=hpo_annotator]{hpo_annotator()}}, or
a path to an index file.}

\item{text}{Character vector of case texts.}

\item{case_id}{Optional character vector of case identifiers. If omitted,
\code{names(text)} are used when present, otherwise sequential identifiers are
generated.}

\item{longest_match}{Passed to \code{\link[=hpo_annotate]{hpo_annotate()}}.}

\item{source}{Source label for the candidate table.}

\item{run_id}{Optional run id to carry forward into candidate rows.}
}
\value{
A candidate table as returned by \code{hpo_candidate_table()}.
}
\description{
Loads or reuses a FastHPOCR annotator and runs \code{\link[=hpo_annotate]{hpo_annotate()}} once per input
text, returning a single comparison-friendly candidate table. This is the
package's \code{tool_only} harness lane.
}
\examples{
if (FALSE) {
  ann <- hpo_annotator("hp.index.gz")
  hpo_extract_candidates(
    ann,
    c(case1 = "Short stature and seizures were reported."),
    longest_match = TRUE
  )
}
}
