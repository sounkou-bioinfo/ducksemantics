% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harness.R
\name{hpo_candidate_table}
\alias{hpo_candidate_table}
\title{Convert FastHPOCR annotations to a harness candidate table}
\usage{
hpo_candidate_table(
  annotations,
  case_id = NA_character_,
  source = "FastHPOCR",
  run_id = NA_character_
)
}
\arguments{
\item{annotations}{A data frame or list returned by \code{\link[=hpo_annotate]{hpo_annotate()}}.}

\item{case_id}{Character scalar identifying the source case. \code{NA} is allowed
for ad hoc use, but real harness runs should pass a stable case id.}

\item{source}{Source label for the candidates.}

\item{run_id}{Optional run id to carry forward into joined outputs.}
}
\value{
A data frame with one row per candidate and columns including
\code{case_id}, \code{candidate_id}, \code{candidate_span}, \code{hpo_id}, \code{hpo_label}, and
offset metadata.
}
\description{
Standardizes annotations returned by \code{\link[=hpo_annotate]{hpo_annotate()}} into the candidate table
used by hybrid model-adjudication prompts and comparison harnesses. The HPO
identifier and label are preserved from FastHPOCR, while \code{candidate_id} gives
each row a stable handle for model keep/drop decisions.
}
\examples{
ann <- data.frame(
  span = "short stature",
  id = "HP:0004322",
  label = "Short stature",
  start = 1L,
  end = 13L,
  start_offset = 0L,
  end_offset = 13L,
  stringsAsFactors = FALSE
)
ann$categories <- I(list(list()))
hpo_candidate_table(ann, case_id = "case-1")
}
