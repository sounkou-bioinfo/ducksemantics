% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harness.R
\name{hpo_adjudicate_candidates}
\alias{hpo_adjudicate_candidates}
\title{Run a candidates-to-model adjudication step}
\usage{
hpo_adjudicate_candidates(
  note,
  candidates,
  runner,
  case_id = NULL,
  provider = NA_character_,
  model = NA_character_,
  mode = "candidates_model",
  prompt_version = "rfasthpocr-candidates-model-v1",
  tool_config = "none",
  run_id = NULL,
  retry_count = 0L,
  estimated_cost_usd = NA_real_,
  error_on_failure = TRUE,
  ...
)
}
\arguments{
\item{note}{Character scalar containing the source clinical text.}

\item{candidates}{Candidate table from \code{hpo_candidate_table()} or raw
annotations from \code{\link[=hpo_annotate]{hpo_annotate()}}.}

\item{runner}{Function taking a single prompt string and returning a character
response. It may also return a list with \code{text} and \code{usage} fields, or a
character vector with a \code{usage} attribute.}

\item{case_id}{Optional case id.}

\item{provider}{Provider label for the run log.}

\item{model}{Model label for the run log.}

\item{mode}{Harness mode label.}

\item{prompt_version}{Prompt version label.}

\item{tool_config}{Tool configuration label or list for the run log.}

\item{run_id}{Optional run id. Generated when omitted.}

\item{retry_count}{Retry count to record in the run log.}

\item{estimated_cost_usd}{Optional cost estimate.}

\item{error_on_failure}{If \code{TRUE}, runner or parse errors stop execution. If
\code{FALSE}, errors are captured in the returned \code{run_log}.}

\item{...}{Additional arguments passed to \code{hpo_adjudication_prompt()}.}
}
\value{
A list with \code{prompt}, \code{raw_response}, \code{adjudication}, and \code{run_log}.
}
\description{
Executes a generic model runner on the prompt produced by
\code{hpo_adjudication_prompt()}, parses the structured JSON response, and returns
run-level metadata suitable for provider/model comparisons. This function is
deliberately runner-agnostic: pass a function that calls \code{piknit::pi_run()}, a
local OpenAI-compatible endpoint, Ollama, vLLM, or any other provider.
}
\details{
The returned \code{run_log} treats token counts and reasoning-token counts as API
usage metadata. The useful clinical audit fields remain in the term-level
adjudication table: \code{decision}, \code{evidence_span}, and \code{short_reason}.
}
\examples{
ann <- data.frame(
  span = "seizures",
  id = "HP:0001250",
  label = "Seizure",
  start = 4L,
  end = 11L,
  start_offset = 3L,
  end_offset = 11L,
  stringsAsFactors = FALSE
)
ann$categories <- I(list(list()))
candidates <- hpo_candidate_table(ann, case_id = "case-1")
json <- jsonlite::toJSON(
  list(
    case_id = "case-1",
    decisions = list(list(
      candidate_id = "case-1:0001",
      candidate_span = "seizures",
      normalized_phrase = "seizures",
      hpo_id = "HP:0001250",
      hpo_label = "Seizure",
      decision = "drop",
      support_type = "none",
      patient_context = "negated",
      evidence_span = "No seizures",
      short_reason = "The note explicitly negates seizures."
    ))
  ),
  auto_unbox = TRUE
)
mock_runner <- function(prompt) json
hpo_adjudicate_candidates("No seizures were reported.", candidates, mock_runner)$adjudication
}
