% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotator.R
\name{hpo_annotate}
\alias{hpo_annotate}
\title{Annotate free text with a FastHPOCR index}
\usage{
hpo_annotate(
  annotator,
  text,
  longest_match = FALSE,
  output = c("data.frame", "list", "python"),
  offsets = c("r", "python")
)
}
\arguments{
\item{annotator}{A \code{fast_hpo_cr_annotator} returned by \code{hpo_annotator()}, or
a path to an index file.}

\item{text}{Character scalar to annotate.}

\item{longest_match}{If \code{TRUE}, ask FastHPOCR to keep only the longest match
among overlapping candidates.}

\item{output}{Return type: an R \code{data.frame}, an R \code{list}, or the raw Python
object.}

\item{offsets}{Coordinate convention for the user-facing \code{start} and \code{end}
columns when \code{output} is not \code{"python"}.}
}
\value{
A data frame by default, with columns \code{span}, \code{id}, \code{label}, \code{start},
\code{end}, \code{start_offset}, \code{end_offset}, and list-column \code{categories}.
}
\description{
Runs the upstream \code{HPOAnnotator$annotate()} method and converts the returned
Python annotation objects to an R data frame by default. Set \code{output = "python"} to keep the raw Python result for advanced reticulate workflows.
}
\details{
FastHPOCR reports offsets as Python-style zero-based, end-exclusive spans.
The default \code{offsets = "r"} adds R-style \code{start} and \code{end} columns that are
one-based and inclusive. The original Python offsets are always preserved as
\code{start_offset} and \code{end_offset}.
}
\examples{
if (FALSE) {
  ann <- hpo_annotator("hp.index.gz")
  hpo_annotate(ann, "The patient has short stature and seizures.")
}
}
