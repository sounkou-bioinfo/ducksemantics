% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/real-index.R
\name{hpo_real_index}
\alias{hpo_real_index}
\title{Download and build a cached full HPO FastHPOCR index}
\usage{
hpo_real_index(
  cache_dir = default_hpo_real_cache_dir(),
  rebuild = FALSE,
  quiet = TRUE,
  root_concepts = "HP:0000118",
  include_top_level_category = TRUE,
  compress_index = FALSE
)
}
\arguments{
\item{cache_dir}{Directory for \code{hp.obo} and the generated \code{index/hp.index}.
Defaults to \code{RFASTHPOCR_REAL_HPO_DIR}; otherwise it reuses an existing
cached full-HPO index when found, falling back to an R user cache directory.}

\item{rebuild}{Re-download/rebuild even when cached files already exist.}

\item{quiet}{Passed to \code{\link[=download_hpo_obo]{download_hpo_obo()}}.}

\item{root_concepts}{HPO root concepts passed to \code{\link[=index_hpo]{index_hpo()}}. The default
indexes all phenotypic abnormalities under \code{HP:0000118}.}

\item{include_top_level_category}{Passed to \code{\link[=index_hpo]{index_hpo()}}.}

\item{compress_index}{Passed to \code{\link[=index_hpo]{index_hpo()}}. Defaults to \code{FALSE} because
upstream FastHPOCR currently writes the full HPO index uncompressed in this
path.}
}
\value{
A normalized index path. Attributes include \code{hpo_file}, \code{cache_dir},
and \code{index_dir}.
}
\description{
Downloads the current real \code{hp.obo} file if needed, builds a FastHPOCR HPO
index if needed, and returns the cached index path. This helper is intended
for real local analyses, README stress tests, and benchmark harnesses; it is
not run during ordinary package tests because full HPO indexing can take
several minutes on a fresh cache.
}
\examples{
if (FALSE) {
  index <- hpo_real_index()
  ann <- hpo_annotator(index)
}
}
