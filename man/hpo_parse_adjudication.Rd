% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harness.R
\name{hpo_parse_adjudication}
\alias{hpo_parse_adjudication}
\title{Parse model adjudication JSON into a data frame}
\usage{
hpo_parse_adjudication(x, candidates = NULL)
}
\arguments{
\item{x}{Character response, a \code{piknit}/Pi reply character vector, or a list
with a \code{text}, \code{content}, or \code{reply} field.}

\item{candidates}{Optional candidate table used to join offsets and source
metadata.}
}
\value{
A data frame with one row per adjudicated candidate.
}
\description{
Parses the structured JSON returned by the candidate adjudication prompt and
validates the core fields. If the original candidate table is supplied,
candidate offsets and source metadata are joined back for audit.
}
\examples{
json <- jsonlite::toJSON(
  list(
    case_id = "case-1",
    decisions = list(list(
      candidate_id = "case-1:0001",
      candidate_span = "seizures",
      normalized_phrase = "seizures",
      hpo_id = "HP:0001250",
      hpo_label = "Seizure",
      decision = "drop",
      support_type = "none",
      patient_context = "negated",
      evidence_span = "No seizures",
      short_reason = "The note explicitly negates seizures."
    ))
  ),
  auto_unbox = TRUE
)
hpo_parse_adjudication(json)
}
