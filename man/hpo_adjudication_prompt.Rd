% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harness.R
\name{hpo_adjudication_prompt}
\alias{hpo_adjudication_prompt}
\title{Build a prompt for candidate-to-model HPO adjudication}
\usage{
hpo_adjudication_prompt(
  note,
  candidates,
  case_id = NULL,
  allow_inferred = FALSE,
  prompt_version = "rfasthpocr-candidates-model-v1",
  extra_instructions = NULL
)
}
\arguments{
\item{note}{Character scalar containing the source clinical text.}

\item{candidates}{Candidate table from \code{hpo_candidate_table()} or raw
annotations from \code{\link[=hpo_annotate]{hpo_annotate()}}.}

\item{case_id}{Optional case id. If omitted, it is inferred from the
candidate table when possible.}

\item{allow_inferred}{If \code{TRUE}, the prompt allows candidate decisions to use
\code{support_type = "inferred"} when clearly supported by the note. It still
asks the model not to invent new HPO IDs.}

\item{prompt_version}{Version label embedded in the prompt.}

\item{extra_instructions}{Optional additional instruction text.}
}
\value{
A single prompt string.
}
\description{
Creates the prompt for the practical hybrid arm:
\verb{note + FastHPOCR candidates -> model keep/drop adjudication}. The model is
asked for auditable short fields, not hidden chain-of-thought.
}
\examples{
ann <- data.frame(
  span = "seizures",
  id = "HP:0001250",
  label = "Seizure",
  start = 4L,
  end = 11L,
  start_offset = 3L,
  end_offset = 11L,
  stringsAsFactors = FALSE
)
ann$categories <- I(list(list()))
candidates <- hpo_candidate_table(ann, case_id = "case-1")
prompt <- hpo_adjudication_prompt("No seizures were reported.", candidates)
cat(substr(prompt, 1, 200))
}
