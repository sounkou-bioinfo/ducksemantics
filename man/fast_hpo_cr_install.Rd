% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/python.R
\name{fast_hpo_cr_install}
\alias{fast_hpo_cr_install}
\title{Install the Python FastHPOCR dependency set}
\usage{
fast_hpo_cr_install(
  envname = "r-fast-hpo-cr",
  method = c("auto", "virtualenv", "conda"),
  source = c("pypi", "github"),
  min_version = FAST_HPO_CR_MIN_VERSION,
  ref = "main",
  ...
)
}
\arguments{
\item{envname}{Reticulate environment name passed to \code{\link[reticulate:py_install]{reticulate::py_install()}}.}

\item{method}{Environment backend passed to \code{\link[reticulate:py_install]{reticulate::py_install()}}.}

\item{source}{Install the released PyPI package or the upstream GitHub source.}

\item{min_version}{Minimum PyPI version when \code{source = "pypi"}. Use \code{NULL}
for unpinned.}

\item{ref}{Git ref when \code{source = "github"}.}

\item{...}{Additional arguments passed to \code{\link[reticulate:py_install]{reticulate::py_install()}}.}
}
\value{
Invisibly, the Python package specifications that were requested.
}
\description{
Installs \code{FastHPOCR} plus its undeclared Python runtime dependencies into a
named reticulate environment. This helper is for users who prefer a persistent
managed environment; ordinary package use relies on \code{\link[reticulate:py_require]{reticulate::py_require()}}
in \code{.onLoad()} and can use reticulate's ephemeral virtual environment.
}
\details{
Set \code{source = "github"} to install from the upstream GitHub repository
subdirectory that contains \code{setup.py}.

If Python has already been initialized in the current R session, restart R
after installing and before loading an annotator so that reticulate can bind
to the intended environment. To select the named environment explicitly, call
\code{fast_hpo_cr_use_env()} before Python is initialized.
}
\examples{
if (FALSE) {
  fast_hpo_cr_install()
  fast_hpo_cr_install(source = "github")
}
}
