% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{download_hpo_obo}
\alias{download_hpo_obo}
\alias{download_mondo_obo}
\alias{download_orphanet_product1_xml}
\alias{download_orphanet_product1_json}
\title{Download ontology source files for FastHPOCR indexing}
\usage{
download_hpo_obo(
  dest_dir = ".",
  filename = "hp.obo",
  overwrite = FALSE,
  url = HPO_OBO_URL,
  quiet = FALSE
)

download_mondo_obo(
  dest_dir = ".",
  filename = "mondo.obo",
  overwrite = FALSE,
  url = MONDO_OBO_URL,
  quiet = FALSE
)

download_orphanet_product1_xml(
  dest_dir = ".",
  filename = "en_product1.xml",
  overwrite = FALSE,
  url = ORPHANET_PRODUCT1_XML_URL,
  quiet = FALSE
)

download_orphanet_product1_json(
  dest_dir = ".",
  xml_file = NULL,
  json_filename = "en_product1.json",
  overwrite = FALSE,
  url = ORPHANET_PRODUCT1_XML_URL,
  quiet = FALSE
)
}
\arguments{
\item{dest_dir}{Directory where the file should be written.}

\item{filename}{Output filename within \code{dest_dir}.}

\item{overwrite}{Replace an existing file.}

\item{url}{Source URL. This is mainly exposed for mirrors and tests.}

\item{quiet}{Passed to \code{\link[utils:download.file]{utils::download.file()}}.}

\item{xml_file}{Existing Orphanet XML file. If \code{NULL}, the XML is downloaded
first with \code{download_orphanet_product1_xml()}.}

\item{json_filename}{Output JSON filename within \code{dest_dir}.}
}
\value{
The normalized path to the downloaded file.
}
\description{
These helpers download commonly used ontology source files into a local
directory and return the resulting path. \code{download_hpo_obo()} downloads
\code{hp.obo}, \code{download_mondo_obo()} downloads \code{mondo.obo}, and
\code{download_orphanet_product1_xml()} downloads Orphanet \code{en_product1.xml}.
}
\details{
FastHPOCR's ORPHANET indexer expects a small JSON shape rather than the XML
served by Orphadata, so \code{download_orphanet_product1_json()} downloads the XML
and converts the fields used by FastHPOCR.
}
\examples{
if (FALSE) {
  hp <- download_hpo_obo("ontologies")
  mondo <- download_mondo_obo("ontologies")
}
if (FALSE) {
  orpha_json <- download_orphanet_product1_json("ontologies")
}
}
